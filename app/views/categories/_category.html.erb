		<tr>
			<th><%= category.name %></th>
			<% if can? :manage, Category %>
			<td><%= category.description %></td>
			<td><%= category.url%></td>
			<td>layouts/<strong><%= category.layout %></strong>.html.erb</td>
			<td class="<%= category.url %>"><%= link_to_if(can?(:edit, Category), "Edit", edit_category_path(category.id)) {} %></td>
			<% end %>
		</tr>