<articles id="list">
<%= render @articles %>
</articles>
<%= will_paginate @articles %>

<a name="add"></a>
<% if user_signed_in? %>
<h3>Something to add?</h3>
<%= render :partial => "form" %>
<% end %>
