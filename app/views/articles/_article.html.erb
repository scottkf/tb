<article>
  <header>
		<h2><%= article.title %></h2>
    <span class="author"><%= article.author.name %></span>
		<span class="date"><%= article.created_at.strftime('%D %T') %>
  </header>
	<section>
		<%= raw(article.rendered_body) %>
	</section>
</article>